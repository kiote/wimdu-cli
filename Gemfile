source 'https://rubygems.org'

gem "aruba"
gem "rspec"

gem "highline" # A higher level command-line oriented interface
gem "sqlite3" # Don't want to use something heavy as a database

gem "activemodel" # too lazy to implement my own db-model wrapper
